# 碎碎念

---

## 源代码操作

生成get/set；方法重写，使子类方法返回对象内容；

## 实体类生成set和get函数

源代码操作-找到类似 `generate getter and setter` 的选项-选择 `all` 即可

## 前置与后置

确实，`C/C++` 在这个上有问题，尽量写 `int[10] arr` （这种是前置，也有后置）这种合在一起的形式，更加简明，也就是尽量写成这种形式： `String[] args`

## 缩写

IDE支持一些特殊的缩写，如下：

| 缩写     | 对应展开代码                               |
| -------- | ------------------------------------------ |
| `sout` | `System.out.println ()`                  |
| `souf` | `System.out.printf()`                    |
| `psvm` | `public static void main(String[] args)` |
| `fori` | `for (int i = 0; i < ; i++)`             |

## 自动重写object的equals

源代码操作中

## clone方法重写

直接类中输出`clone`即可
