/**
 * 周报文件太多了好像有点问题，没法编译，但是单独复制出去可以运行
 * testMain
 */
public class testMain {
    public static void main(String[] args) {
        classManager c = new classManager();
        c.printInfo();
    }
}
