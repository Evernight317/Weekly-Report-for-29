<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.tlias.mapper.EmpMapper">
    <delete id="delete">
        DELETE FROM emp WHERE id IN
        <foreach item="id" collection="ids" separator="," open="(" close=")">
                #{id}
        </foreach>
    </delete>

    <!-- 条件查询（分页由 PageHelper 自动添加 limit） -->
    <select id="list" resultType="org.example.tlias.pojo.Emp">
        SELECT * FROM emp
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="gender != null">
                AND gender = #{gender}
            </if>
            <if test="begin != null">
                AND hire_date >= #{begin}
            </if>
            <if test="end != null">
                AND hire_date &lt;= #{end}
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>